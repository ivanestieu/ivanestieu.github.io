{
  "name": "veste3d",
  "version": "1.0.0",
  "description": "Présentation interactive d'une veste 3D en Three.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx gh-pages -d dist -u github-actions[bot]\\ <github-actions[bot]@users.noreply.github.com> -r https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.2.0",
    "vite-plugin-gh-pages": "^1.0.1"
  }
}
